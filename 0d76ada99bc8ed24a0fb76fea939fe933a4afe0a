{"category": "Changed", "changelog_entry": "Optimized shell scripts to use tabs consistently for improved readability and standard indentation style.", "summary": "Shell scripts were reindented to use tabs consistently, improving readability and enforcing a standard indentation style. This change does not affect functional behavior but ensures consistent formatting across scripts, reducing potential confusion and editor interpretation issues. The reindentation applies to scripts in the `Makefile` and `ci/update_versions.sh` files, as well as the `scripts/configure_qmd.sh` and `scripts/qmd_session_start.sh` scripts.", "version": 1}
