---
layout: default
title: Stanford CME295 Transformers & LLMs | Autumn 2025 | Lecture 5 - LLM tuning
nav_order: 5
parent: Large Language Models Llms
type: Video Note
description:
  In this lecture, the instructor reviews the process of tuning large language
  models (LLMs), focusing on the stages beyond pre-training and supervised fine-tuning
  (SFT). After pre-training teaches the model general language and code patterns...
resource: https://www.youtube.com/watch?v=PmW_TMQ3l0I
tags:
  - large-language-models-llms
  - video
  - learning
status: stable
generated:
  by: process:yt-conferences-okf
  at: 2026-09-12 00:00:00+00:00
---

# Summary

In this lecture, the instructor reviews the process of tuning large language models (LLMs), focusing on the stages beyond pre-training and supervised fine-tuning (SFT).
After pre-training teaches the model general language and code patterns, SFT adapts it to specific tasks, but may not fully align outputs with human preferences.
To address this, a third stage called preference tuning is introduced, where models are trained to prefer outputs that humans rate as better, typically using pairwise preference data.
The lecture explains how reinforcement learning from human feedback (RLHF) uses a reward model trained on these preferences to further align the LLM, employing algorithms like Proximal Policy Optimization (PPO) to balance reward maximization with staying close to the base model and avoid issues like reward hacking.
The instructor also discusses practical alternatives such as "best of n" selection and Direct Preference Optimization (DPO), which directly optimizes model weights using preference pairs without a separate reward model, offering a simpler but sometimes less optimal approach.
Throughout, the lecture highlights the trade-offs, challenges, and practical considerations in aligning LLMs with nuanced human values and preferences.

# Main Points

|   # | Main point                                                                                                                                                                                         |
| --: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|   1 | Midterm exam and solutions are posted; final exam will have similar format, covering lectures 5–9.                                                                                                 |
|   2 | Lecture topic: LLM tuning, focusing on preference alignment.                                                                                                                                       |
|   3 | Recap: LLM training involves pre-training (large, expensive, data-heavy, teaches language/code structure) and fine-tuning/SFT (supervised, smaller, high-quality dataset, task-specific behavior). |
|   4 | LoRA introduced as a parameter-efficient fine-tuning method.                                                                                                                                       |
|   5 | Preference tuning (third step) aligns model outputs with human preferences, addressing tone, safety, and other qualitative aspects.                                                                |
|   6 | Preference pairs: For a given prompt, collect two responses (one preferred, one not); used to teach the model which outputs are better.                                                            |
|   7 | Preference data collection: Pointwise (score each response), pairwise (choose better of two), listwise (rank multiple); pairwise is most common.                                                   |
|   8 | Preference pairs can be generated by sampling model outputs with positive temperature and rated by humans or automated metrics.                                                                    |
|   9 | RLHF (Reinforcement Learning from Human Feedback) uses preference pairs to train a reward model, then aligns the LLM using RL.                                                                     |
|  10 | In RLHF, the LLM is the agent, state is the input so far, action is next token prediction, reward comes from the reward model.                                                                     |
|  11 | Reward signals in RLHF are sparse (one per completion), unlike dense SFT signals.                                                                                                                  |
|  12 | Reward model is trained using the Bradley-Terry formulation and a pairwise loss (negative log-sigmoid of reward difference).                                                                       |
|  13 | Reward model outputs a scalar score for a prompt-response pair; can be interpreted as classification or regression.                                                                                |
|  14 | RLHF policy optimization uses PPO (Proximal Policy Optimization) with a loss combining reward maximization and KL divergence penalty to prevent deviation from the base model.                     |
|  15 | KL divergence measures distance between current and reference policy distributions.                                                                                                                |
|  16 | Value function estimates expected reward at the token level; advantage function (reward minus baseline) stabilizes training.                                                                       |
|  17 | PPO loss variants: PPO-Clip (limits update size                                                                                                                                                    |

# Video

[Watch on YouTube](https://www.youtube.com/watch?v=PmW_TMQ3l0I)
